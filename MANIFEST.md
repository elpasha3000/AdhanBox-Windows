# 🖥️ AdhanBox for Windows — مانفست المشروع

> بدأ 2026-07-29 بقرار أحمد. تطبيق أذان **مجاني** لويندوز → متجر مايكروسوفت + GitHub (كود مفتوح MIT).
> **ابدأ من هنا دايمًا.** الشقيق: AdhanBox للراسبيري (`D:\WORK\Athan-Docker-Project`).

## 📌 قرارات أحمد النصية (2026-07-29)
- بسيط **زي صفحة الراسبيري**: مواقيت اليوم + عدّاد الصلاة الجاية + إعدادات لكل صلاة + تجربة صوت.
- **عربي/إنجليزي**.
- تراي + إقلاع تلقائي. **⚠️ أهم شيء: ممنوع نقع مع ويندوز ديفندر تاني** (عقدة Wacatac).
- تتبع لماجيك ويب: نعرف مين جه للموقع من التطبيق.
- طلب «أوبشن ويندوز سيرفس» → **اتشرح له إن المتجر بيمنع الخدمات في MSIX + صوت session-0 مش مضمون**، والبديل: إقلاع تلقائي (بيغطي الجهاز المقفول). نسخة GitHub بمثبّت حر + سيرفس = خيار مستقبلي لو ظهر طلب. *(قال «ماشي» ضمنيًا بمواصلة الشغل — أكّد معاه قبل تسليم نهائي)*

## 🛡️ استراتيجية ديفندر (غير قابلة للتفاوض)
1. **التوزيع الأساسي = متجر مايكروسوفت فقط** — MSIX موقّعة من مايكروسوفت → ديفندر بيثق تلقائيًا.
2. GitHub = **كود مصدري + بادج المتجر**، مفيش exe حر في المرحلة الأولى.
3. صفر ضغط/تشفير/obfuscation · موارد نسخة كاملة · مانيفست سليم · static CRT (راجع `build-guards-and-gotchas` في ذاكرة كلود).

## 🏗️ المعمار
- **C++ Win32 نيتف** (بدون .NET) — نفس أسلوب Screen2ipcam (admin_gui نمط مرجعي).
- الصوت: أذان الحرم مضمّن (`makkah-adhan.mp3` — نفس ملف مشروع الراسبيري، أحمد أكّد بدون حقوق) + إضافة ملفات. تشغيل MP3: MCI (`mciSendString`) — شغّال 7→11.
- المدن: `cities.json` بتاع الراسبيري (67 دولة/264 مدينة بأسماء AR+EN) — يتحوّل لجدول C++ مولّد.
- التتبع: لينكات `?src=adhanbox-win` (صفر مخاطرة) + لاحقًا نبضة إصدار اختيارية بسياسة خصوصية.
- الحفظ: `%ProgramData%\MagicWeb\AdhanBox\` أو `%LocalAppData%` (MSIX بيوجّه لوحده) — **مش Program Files** (درس باج #4 بتاع Screen2ipcam).

## 🏁 حالة 2026-08-03 ليلًا — v0.4.1 + الحزمة جاهزة (آخر كلمة)

**الحزمة:** `msix\AdhanBox_0.4.0.0_x64.msix` (732KB · **غير موقّعة عن قصد** — مايكروسوفت بتوقّع) · هوية المتجر: `Screen2ipcam.AdhanBox` / `CN=759F2B5B-...` / PFN `Screen2ipcam.AdhanBox_0qnf38brt1ger` / Store ID `9PHP7QSVQ829`. البناء: `msix\build-msix.bat` (SDK في `D:\Windows Kits\10\bin\10.0.22621.0\x64` — **مش C:**).

**اتصلّح وجُرّب لايف قدام أحمد:** النافذة المصغّرة ماكانتش بتطلع وقت الأذان (`IsWindowVisible` بيعتبر المصغّرة «ظاهرة») → `SW_RESTORE` + topmost لحظي + `FlashWindow`. التجربة: فتحت 22:33:00 في الواجهة، أذّنت 20ث، اختفت 22:33:20 لوحدها.

**تغييرات MSIX الجوهرية:** الأذان **مدمج** جوّه الـexe (RCDATA 101، الـexe بقى 804KB) وبيتستخرج لـ`%LOCALAPPDATA%\MagicWeb\AdhanBox\sounds` (مجلد تثبيت MSIX **للقراءة فقط** — كان هيكسر الصوت تمامًا) + نقل تلقائي لأصوات المستخدم القديمة + `AdhanBoxStartup.exe` (نمط s2c_start) للـStartupTask بيشغّل `/tray`.

**⚠️ ملحوظة اختبار:** تفعيل الحزمة من الحاوية بوضع المطوّر (loose registration) فشل صامتًا على الجهاز ده (AppModel 0x490) — **مش عيب في الحزمة**: نفس نمط Screen2ipcam 1.1.5 اللي عدّى تصديق المتجر، والـexe والمشغّل متجرّبين مباشرة. المتجر بيثبّت موقّع في WindowsApps فالمسار مختلف. لو عايز تعيد التجربة: وضع المطوّر مفعّل عند أحمد.

**الرفع لـPartner Center:** نصوص الصفحة في `D:\WORK\AdhanBox-Store-Listing\` (AR/EN متحقّق من الحدود) + privacy URL: `https://github.com/elpasha3000/AdhanBox-Windows/blob/main/PRIVACY.md` + الصور في `store\`. **الريبو عام ومدفوع:** github.com/elpasha3000/AdhanBox-Windows (آخر دفعة `82c752b`).

## 🏁 حالة 2026-07-30 ليلًا — v0.4 (آخر كلمة — ابدأ من هنا)

**commit `4217e11`** (محلي، لسه ما اتدفعش). اللي اتزاد فوق v0.3 بطلب أحمد:
- **ساعة حيّة** بالساعات والدقايق والثواني تحت اسم البرنامج (مؤقّت 1 ثانية، بيعيد رسم كارت الرأس بس)، و**بعد آخر صلاة بيعدّ لفجر بكره** بدل رسالة ساكنة.
- **زرار «✔ تطبيق»** جنب الترحيل العام: بيوزّع الرقم على خانة ±دقيقة بتاعة كل صلاة ويرجّع العام صفر (متفحوص: +3 → كل الخانات 3 والعام 0). ومعاه **زراير − و +**.
- **زراير − و + لدرجة صوت كل أذان** (خطوة 5)، وبتغيّر الصوت فورًا لو الأذان شغّال.
- **مدة التشغيل لكل أذان**: كومبو (كامل / 15 / 30 / 45 / 60 / 90 / 120 / 180 / 300 ثانية) → `g_stopAt` بيوقّف في الوقت المحدد.
- **شاشة أبوت جديدة** (نافذة مرسومة مش MessageBox): أيقونة + الإصدار + سطر «نسألكم الدعاء» + **MagicWeb سطر لوحدها** + `magicweb.win` و`AdhanBox@magicweb.win` **لينكات شغّالة** (مؤشّر اليد + ShellExecute/mailto) + جيت هب آخر سطر كنص عادي + زرار إغلاق.
- **شريط عنوان غامق** لكل النوافذ (`DwmSetWindowAttribute` 20 ثم 19) — الشكل بقى متجانس ومناسب لصور المتجر.
- نافذة الإعدادات بقت **600px** (أسماء ملفات الصوت كانت بتتقص) + **مقيّدة جوّه الشاشة** + ليبل «± دقيقة» في اللغتين.
- إصلاح `&` في نصوص الإنجليزي (DrawText كان بياكل الحروف) → `DT_NOPREFIX`.

**📸 صور المتجر جاهزة:** `store/` فيها 6 صور **1920×1080** (عربي وإنجليزي × رئيسية/إعدادات/أبوت) + `make_store_shots.ps1` (بيعيد توليدها من لقطات `p_*.png`) + `captions.txt` (النصوص UTF-8 — تتقرا بـ`Get-Content -Encoding UTF8` عشان العربي).

**⚠️ درس:** الأرقام في الريجستري بتتخزّن بإزاحة +60 (`pr*_tune`, `global_tune`) — القيمة 60 = صفر. وأحمد بيغيّر إعداداته بنفسه أثناء الشغل، متفترضش إن أي فرق = باج.

## 🏁 حالة 2026-07-30 (v0.3)

**v0.3 اتبنت واتفحصت لايف.** طلبات أحمد الستة اتنفذت كلها:

| الطلب | التنفيذ | الفحص |
|---|---|---|
| يقوم مصغّر مع ويندوز | مفتاح Run بقى `"...AdhanBox.exe" /tray` + فحص الأرجيومنت في `wWinMain` | ✅ اتشغّل بـ`/tray` والنافذة رجّعت HIDDEN |
| تظهر الشاشة وقت الأذان وتختفي بعده | `g_showAtAdhan` + `g_autoShown` + `mciStillPlaying()` في tick كل ثانيتين + `hideIfAutoShown()` | ✅ **اختبار حي**: ضبطت المغرب على 20:16 بصوت 5 → النافذة ظهرت لوحدها 20:16:06، وبعد إيقاف الصوت رجعت HIDDEN |
| زرار الصوت في الإعدادات توجل | `togglePlay()` — ▶ أخضر ↔ ■ أحمر، نفس الزرار بيوقّف | ✅ سكرين شوت للحالتين |
| أيقونة البرنامج | `assets/adhanbox.ico` (مسجد أبيض على تدرّج أزرق→أخضر، 8 مقاسات 16→256) + `src/app.rc` + بلوك إصدار | ✅ بانت في العنوان والتراي وبتقرا لحد 16px |
| زرار إغلاق تحت في الإعدادات | `BT_CLOSE` — بيحفظ ويقفل | ✅ |
| زرار «أضف ملفات أذان» جنب فتح المجلد | `addSoundFiles()` بـ`GetOpenFileNameW` multi-select، بيفتح على **مجلد التنزيلات**، بينسخ لمجلد الأصوات وبيحدّث القوايم فورًا | ✅ الديالوج فتح على Downloads بفلتر ملفات الصوت |
| **ترحيل عام** تحت طريقة الحساب | `g_globalTune` (±120 دقيقة) بيتضاف لكل الصلوات **والشروق** | ✅ حطيت 5 → الستة مواقيت طابقوا المحرّك المرجعي (04:39/06:18/13:07/16:43/19:55/21:22) |

**كمان في v0.3:** إصلاح **باج ريجستري** — `Software\MagicWeb\AdhanBox` كان فاقد الباكسلاشات (فخ الهروب) فالقيم النصية كانت بتتخزّن في مفتاح غلط `HKCU\SoftwareMagicWebAdhanBox`؛ اتصلّح واتنقلت قيمة أحمد (hamza.mp3 للفجر) واتمسح المفتاح اليتيم. وكمان `.gitignore` (سورس فقط — مفيش exe في الريبو، سياسة ديفندر) وحُرّاس فشل في `build.bat`.

**⏳ الباقي:** (1) **تخطيط الإعدادات مش متعكس في الوضع الإنجليزي** — الدولة/المدينة ومذهب العصر/الطريقة لسه بترتيب RTL في EN، مخالف لقاعدة `rtl-layout-mirroring` (مؤجّل: أحمد مطلبوش والرصيد محدود) · (2) دفع لريبو `AdhanBox-Windows` على GitHub · (3) MSIX + المتجر · (4) README.

## 🏁 حالة 2026-07-29 ليلًا
**v0.2 كاملة الوظائف واتبنت واشتغلت وأذّنت لايف.** فيها: صفحة إعدادات كاملة زي الراسبيري (دولة/مدينة 264 بكومبو مترابطة، 20 طريقة، مذهب العصر، ولكل صلاة: صوت/درجة/±دقيقة/أون-أوف/▶ تجربة) + 3 مفاتيح عامة (أذان تلقائي/تنبيه 10د/إقلاع مع ويندوز) + كتم ساعة و24 ساعة من التراي + عربي/إنجليزي كامل + About بإيميل AdhanBox@magicweb.win + «الكود مجاني — نسألكم الدعاء». الحفظ في `HKCU\Software\MagicWeb\AdhanBox`. **commit محلي `c3371a3`** (git جوّه المجلد — **لسه ما اتدفعش لريبو GitHub**). الباقي: أيقونة براند، دفع لريبو `AdhanBox-Windows`، MSIX/المتجر، وفحص بصري للوضع العربي للإعدادات. فخ متكرر: نصوص C فيها `\` تتكتب بـEdit tool مش python heredoc.

## ✅ الحالة
| بند | حالة |
|---|---|
| **محرّك المواقيت C++** (`engine/athan_times.h/.cpp`) | ✅ **منقول ومتحقق: 1680 مقارنة ضد البايثون المرجعي = IDENTICAL** (7 مدن منها أوسلو عرض-عالي × 5 طرق × 4 تواريخ منها الانقلابين × مذهبي العصر) — `engine/verify.py` بيعيد الاختبار في أي وقت (build_test.bat) |
| الواجهة (نافذة + تراي) | ⏳ الجاية — بعد تصفير الرصيد |
| تحويل cities.json لجداول C++ | ⏳ |
| تشغيل الصوت + الجدولة | ⏳ |
| MSIX + حجز اسم بالمتجر | ⏳ (أحمد عنده Partner Center جاهز من S2C_STORE) |
| README + GitHub repo | ⏳ (اسم مقترح: `AdhanBox-Windows` تحت elpasha3000) |

## ⚠️ ملاحظات تقنية
- بيئة البناء: `vcvars64.bat` بتاع VS 18 Community (نفس بيئة Screen2ipcam) — `engine/build_test.bat` نموذج.
- المحرّك بيرجّع NAN لو الظاهرة مش بتحصل (عروض عالية) ومعالج AngleBased منقول بالحرف — متغيّرش فيه من غير إعادة تشغيل verify.py.
- التقريب للدقيقة: `llround(h*60)` مطابق لبايثون `int(round(v*60))` — اتأكد من ده في الاختبار فعليًا.
- قاعدة أحمد: **صفر وكلاء** — الشغل مباشر، ووضع توفير توكنز دائم.
